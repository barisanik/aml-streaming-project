aml_platform:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5432
      user: app_dbt
      password: "{{ env_var('APP_DBT_DB_PASSWORD') }}"
      dbname: aml_platform
      schema: analytics
      threads: 4
      timeout_seconds: 300